# Telegram Video Inbox Bot

> **Русская версия** | **[English](README.md)**

Telegram бот для приема видео и управления файлами на Android TV-box (X96Q, slimBOXtv и аналогичных устройствах) с использованием Termux.

## Возможности

- 📥 **Прием видео**: отправьте видео боту → оно сохраняется на TV-box
- 📁 **Управление файлами**: просмотр списка, скачивание, удаление через Telegram
- 🔒 **Whitelist доступ**: только авторизованные пользователи
- ♾️ **Без ограничений размера**: использует локальный Bot API server
- ⚡ **Атомарная запись**: безопасное сохранение файлов
- 🚀 **Автозапуск**: работает с Termux:Boot

## Архитектура

```
┌─────────────┐         ┌──────────────────┐         ┌────────────┐
│   Telegram  │  HTTPS  │   Telegram API   │  HTTP   │  Bot API   │
│   Servers   │◄───────►│     (Cloud)      │◄───────►│   Server   │
└─────────────┘         └──────────────────┘         │  (Local)   │
                                                      └──────┬─────┘
                                                             │ HTTP
                                                      ┌──────▼─────┐
                                                      │  Python    │
                                                      │    Bot     │
                                                      │  (PTB)     │
                                                      └──────┬─────┘
                                                             │
                                                      ┌──────▼─────┐
                                                      │  SHARED_   │
                                                      │    DIR     │
                                                      └────────────┘
```

Весь стек работает локально на TV-box в Termux.

**Framework:** python-telegram-bot (PTB) - стандартная Telegram Bot API библиотека для Python, без зависимости от pydantic-core.

## Требования

### Устройство
- Android TV-box (X96Q, slimBOXtv, или аналогичные)
- Android 7.0+
- Доступ к shared storage
- Подключение к интернету

### ПО
- **Termux** (из F-Droid - обязательно!)
- **Termux:Boot** (из того же источника)
- Python 3.8+
- Git
- **ffmpeg** (для корректной работы с метаданными видео)

## Быстрый старт

### Подготовка

Перед установкой вам нужно собрать следующую информацию:

1. **Создать Telegram бота**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot` и следуйте инструкциям
   - Сохраните **токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Получить Telegram API credentials**
   - Посетите https://my.telegram.org
   - Войдите с помощью номера телефона
   - Перейдите в **API development tools**
   - Создайте новое приложение:
     - **App title**: `TV-box Video Inbox`
     - **Short name**: `tvbox`
     - **Platform**: `Other`
   - Сохраните **api_id** (число) и **api_hash** (строка)

3. **Узнать свой Telegram User ID**
   - Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте любое сообщение
   - Сохраните свой **user ID** (число)

4. **Знать характеристики устройства** (опционально, для диагностики)
   - Модель устройства (например, X96Q, slimBOXtv)
   - Версия Android

### Установка

> [!IMPORTANT]
> **Устанавливайте Termux ТОЛЬКО из F-Droid!**
> 
> Версия из Google Play устарела и несовместима.
> - F-Droid: https://f-droid.org/
> - Termux: https://f-droid.org/packages/com.termux/
> - Termux:Boot: https://f-droid.org/packages/com.termux.boot/

**Установка одной командой:**

```bash
# 1. Установите Termux из F-Droid, затем откройте его

# 2. Дайте доступ к хранилищу
termux-setup-storage
# Разрешите при запросе

# 3. Клонируйте репозиторий
cd ~
git clone https://github.com/Spiceman161/Telegram-video-inbox.git
cd Telegram-video-inbox

# 4. Запустите автоматическую установку
chmod +x scripts/install_dependencies.sh
./scripts/install_dependencies.sh
```

Скрипт выполнит:
- Установку всех системных зависимостей (Python, Git, ffmpeg, инструменты сборки)
- Сборку Telegram Bot API Server (~45 минут на TV-box)
- Установку Python пакетов
- Проведёт вас через процесс настройки
- Создаст все необходимые директории

**Во время установки** вас попросят ввести:
- Токен бота (из шага 1)
- API ID и API Hash (из шага 2)
- Ваш user ID (из шага 3)
- Путь для хранения видео (по умолчанию: `/storage/emulated/0/Movies/TelegramInbox`)

### Первый запуск

После завершения установки:

```bash
# Запустите Bot API Server (в первом окне терминала)
./scripts/start_bot_api.sh &

# Подождите 5 секунд для инициализации
sleep 5

# Запустите бота (во втором окне терминала или используйте tmux/screen)
./scripts/start_bot.sh &
```

**Проверьте бота:**
1. Откройте Telegram на телефоне
2. Найдите вашего бота
3. Отправьте `/start` - должно появиться приветственное сообщение
4. Отправьте видео - оно должно сохраниться на TV-box

## Использование

### Команды бота

- `/start` - начать работу, показать главное меню

### Кнопки

- **📥 Inbox** - просмотр файлов
- **⬆️ Статус** - статистика системы
- **❓ Помощь** - справка

### Отправка видео

Просто отправьте или перешлите любое видео боту. Оно автоматически сохранится в `SHARED_DIR`.

### Управление файлами

1. Нажмите **📥 Inbox**
2. Выберите файл из списка
3. **⬇️ Скачать** - получить обратно в Telegram
4. **🗑 Удалить** - удалить с диска (с подтверждением)

## Настройка автозапуска

Чтобы бот автоматически запускался при загрузке TV-box:

```bash
# 1. Установите Termux:Boot из F-Droid

# 2. Отключите оптимизацию батареи
# Настройки → Приложения → Termux → Батарея → Без ограничений
# Настройки → Приложения → Termux:Boot → Батарея → Без ограничений

# 3. Скопируйте boot скрипт
mkdir -p ~/.termux/boot
cp scripts/termux_boot_template.sh ~/.termux/boot/01-telegram-video-inbox.sh
chmod +x ~/.termux/boot/01-telegram-video-inbox.sh

# 4. Откройте приложение Termux:Boot хотя бы раз

# 5. Перезагрузите устройство
```

После перезагрузки бот будет готов через ~60 секунд.

## Конфигурация

Все настройки находятся в файле `.env`:

### Обязательные параметры

```env
# Токен от @BotFather
BOT_TOKEN=your_bot_token_here

# API credentials от my.telegram.org
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash

# Whitelist (ваш Telegram ID, можно узнать у @userinfobot)
ALLOWED_USER_IDS=123456789

# Путь к папке для видео (shared storage)
SHARED_DIR=/storage/emulated/0/Movies/TelegramInbox

# Временная папка
TMP_DIR=/data/data/com.termux/files/home/telegram-video-inbox/tmp
```

### Опциональные параметры

```env
PAGE_SIZE=10                    # Файлов на странице
MAX_CONCURRENT_DOWNLOADS=2      # Параллельных загрузок
SEND_AS=video                   # или 'document'
LOG_LEVEL=INFO
```

## Решение проблем

### Бот не отвечает

```bash
# Проверьте, запущен ли Bot API server
ps aux | grep telegram-bot-api

# Проверьте, запущен ли бот
ps aux | grep "python.*main.py"

# Проверьте логи
tail -f ~/telegram-video-inbox/logs/bot.log
```

### Ошибка "Failed to get bot info"

Bot API server не запущен или недоступен на порту 8081.

```bash
# Перезапустите Bot API server
./scripts/start_bot_api.sh &
```

### Файлы не сохраняются

```bash
# Проверьте права доступа
ls -ld /storage/emulated/0/Movies/TelegramInbox

# Проверьте свободное место
df -h /storage/emulated/0

# Проверьте логи
grep "download_failed" ~/telegram-video-inbox/logs/bot.log
```

### После перезагрузки не запускается

1. Убедитесь, что оптимизация батареи отключена
2. Проверьте, что Termux:Boot был открыт хотя бы раз
3. Проверьте boot скрипт:
   ```bash
   ls -la ~/.termux/boot/
   cat ~/.termux/boot/01-telegram-video-inbox.sh
   ```

## Безопасность

### Whitelist

Только пользователи из `ALLOWED_USER_IDS` могут:
- Загружать файлы
- Просматривать список
- Скачивать и удалять файлы

Остальные получат "🚫 Доступ запрещён" без утечки информации.

### Защита от Path Traversal

Все пути проверяются на выход за пределы `SHARED_DIR`.

### Логирование

Все действия логируются с user_id:
- `upload_received` - видео получено
- `download_ok` - загрузка завершена
- `file_sent` - файл отправлен пользователю
- `file_deleted` - файл удален
- `unauthorized_access` - попытка неавторизованного доступа

Логи: `logs/bot.log`

## Продвинутое использование

### Ручная установка

См. подробные инструкции в [docs/INSTALLATION.md](docs/INSTALLATION.md)

### Использование tmux/screen

Для удобного управления сессиями:

```bash
pkg install tmux

# Создать сессию
tmux new -s telegram

# В tmux:
# Ctrl+B затем C - новое окно
# Ctrl+B затем N - следующее окно
# Ctrl+B затем D - отсоединиться

# Подключиться обратно
tmux attach -t telegram
```

## Участие в разработке

Приветствуются вклады в проект! См. [docs/PRD.md](docs/PRD.md) для понимания требований к проекту.

## Лицензия

MIT License - см. файл [LICENSE](LICENSE) для подробностей.

## Автор

Создано для развертывания на X96Q/slimBOXtv TV-box.

## Поддержка

- **Issues**: Сообщайте об ошибках и запросах функций в GitHub Issues
- **Документация**: См. папку [docs/](docs/) для подробных руководств
- **История изменений**: См. [CHANGELOG.md](CHANGELOG.md) для истории версий
