PRD: Telegram Video Inbox для TV-box
1) Обзор и цель
Продукт: Telegram Video Inbox (бот для приёма видео и управления файлами на Android TV-box).
Цель: пользователь пересылает боту видео в Telegram → бот сохраняет файл в заданную shared-папку на TV-box; пользователь управляет файлами из Telegram (просмотр списка, скачивание, удаление).

Ключевое решение по файлам: используем локальный Telegram Bot API server в режиме --local, чтобы скачивать файлы без ограничения размера и работать с локальными путями.

2) Контекст и платформа
Устройство: X96Q, slimBOXtv ATV 10.8 (Android TV), ядро 4.9.170.
Окружение: Termux (желательно из F-Droid), автозапуск через Termux:Boot.
​
Архитектурное требование: бот и локальный Bot API server запускаются на TV-box (без VPS).

3) Персоны и права доступа
Персона: владелец TV-box и его доверенные пользователи.

Модель доступа (Whitelist):

Доступ к боту разрешён только пользователям из белого списка по Telegram user_id.

Любой входящий апдейт (сообщение, callback query) обрабатывается только после проверки whitelist.

Для неавторизованных: ответ “Доступ запрещён” без утечек информации о файлах/папках.

4) Основные сценарии (User Stories)
US-1: Приём видео

Как доверенный пользователь, пересылаю боту видео (тип video), чтобы оно появилось в папке на TV-box.

US-2: Просмотр файлов

Как доверенный пользователь, открываю “Inbox”, чтобы увидеть список файлов в папке.

US-3: Скачивание

Как доверенный пользователь, выбираю файл и нажимаю “Скачать”, чтобы получить его обратно в Telegram.

US-4: Удаление

Как доверенный пользователь, выбираю файл и удаляю его из папки (с подтверждением), чтобы освободить место.

US-5: Статус

Как доверенный пользователь, открываю “Статус”, чтобы увидеть свободное место и состояние сервиса.

5) UX и интерфейс управления (reply + inline)
5.1 Reply-клавиатура (главное меню)
Интерфейс управления делается через ReplyKeyboardMarkup (кнопки “под полем ввода”).
​
Клавиатура должна быть постоянной и компактной:

is_persistent=true — чтобы Telegram-клиент всегда показывал меню.
​

resize_keyboard=true — чтобы клавиатура занимала меньше места.
​

Кнопки V1 (Reply):

📥 Inbox — открыть/обновить список файлов (живое сообщение).

⬆️ Статус — экран статуса (живое сообщение).

❓ Помощь — краткая справка/правила (живое сообщение).

5.2 Inline-кнопки (выбор конкретных файлов)
Выбор файла и действия над конкретным файлом реализуются inline-кнопками, прикреплёнными к “живому” сообщению списка.

Важно: обработчик inline нажатий обязан отвечать на callback (через answerCallbackQuery), чтобы не висел прогресс у клиента. (Требование фиксируется как обязательное поведение UI.)

5.3 Один “живой” список (единое сообщение)
В чате с пользователем бот поддерживает одно сообщение “список файлов”, которое постоянно обновляется через редактирование:

Если “живое сообщение” ещё не создано: бот отправляет новое сообщение и сохраняет его message_id.

Если уже есть: бот обновляет его содержимое (текст/inline-клавиатуру), не создавая новых сообщений.

Хранить для каждого пользователя:

chat_id

live_list_message_id

last_page (опционально)

6) Функциональные требования
6.1 Приём и сохранение видео
Обрабатывать входящие сообщения, где присутствует message.video (пересланное “как видео”).

Скачивать файл через Bot API (getFile) и сохранять в целевую папку SHARED_DIR.

Атомарная запись:

скачивание в TMP_DIR / *.part

затем rename() в финальное имя.

Генерация имени:

если доступно исходное имя — использовать (после очистки),

иначе YYYY-MM-DD_HH-mm-ss_<file_unique_id>.mp4 (расширение может быть определено по MIME/имени).

Защита от коллизий:

при совпадении имени добавлять суффикс ((1), (2) …) либо короткий хэш.

Лимит параллелизма:

параметр MAX_CONCURRENT_DOWNLOADS (V1: по умолчанию 1–2, чтобы не убить слабый I/O).

6.2 Управление файлами в shared-папке
Источник истины — файловая система SHARED_DIR.

Операции:

Список файлов:

сортировка по mtime (новые сверху) или по имени (настраиваемо),

пагинация (например, PAGE_SIZE=10..20).

Скачать файл:

отправка пользователю в Telegram (как sendDocument по умолчанию, либо sendVideo — опция).

Удалить файл:

подтверждение удаления (inline экран подтверждения),

затем удаление в ФС.

6.3 Безопасность путей
Любой путь должен быть проверен на выход за пределы SHARED_DIR (path traversal защита).

В callback_data нельзя хранить полный путь или длинные идентификаторы; использовать короткий ID.

6.4 Белый список (whitelist)
Whitelist задаётся в конфиге как список Telegram ID.

Любая команда/кнопка/входящий файл проверяется на принадлежность к whitelist.

6.5 Статус и мониторинг
Экран “Статус” (в живом сообщении) должен показывать:

свободное место в разделе, где лежит SHARED_DIR,

количество файлов,

размер папки,

очередь/количество активных скачиваний,

аптайм процессов (опционально).

6.6 Логи и аудит
Логировать:

timestamp

user_id

событие: upload_received, download_started, download_ok, download_failed, list, file_sent, file_deleted, unauthorized_access

имя файла/ID файла

ошибки (stack/error string)

Хранилище логов: файл + (опционально) SQLite.

7) Техническая архитектура (Variant A: всё локально на TV-box)
7.1 Компоненты
Telegram Bot API server (локальный)

Исходники/сборка: CMake, зависимости OpenSSL, zlib, C++17 компилятор, gperf.

Запуск:

обязательные параметры: --api-id, --api-hash (или env TELEGRAM_API_ID, TELEGRAM_API_HASH).

включить --local.

Возможности --local (фиксируем как требования к режиму):

download без лимита размера,

upload до 2000 MB,

возврат абсолютного локального file_path из getFile без необходимости отдельной докачки после getFile.

HTTP-only: сервер принимает только HTTP запросы (не HTTPS).

Порт по умолчанию 8081 (настраивается).

Бот-приложение (Python + aiogram)

AsyncIO runtime.

Long polling через getUpdates (без webhook), обращение к локальному Bot API server по HTTP.

Обработка типов апдейтов:

message (video + reply-кнопки)

callback_query (inline-кнопки)

Обязательное поведение:

корректная обработка offset в getUpdates (без дубликатов/потерь).

7.2 Миграция бота на локальный сервер
Если бот ранее работал через https://api.telegram.org, перед переводом на локальный сервер требуется вызвать logOut, чтобы гарантировать получение всех updates локально.

8) Развёртывание и автозапуск (Termux)
8.1 Termux:Boot
Использовать Termux:Boot для старта сервисов после загрузки устройства:

Скрипты размещаются в ~/.termux/boot/ и выполняются после boot.
​

Требования эксплуатации:

Не смешивать установки Termux и аддонов между Google Play и F-Droid (совместимость подписи).
​

Отключить battery optimizations для Termux и Termux:Boot.
​

В boot-скрипте вызывать termux-wake-lock в начале.
​

8.2 Управление процессами
Варианты:

Минимально: запуск обоих процессов (Bot API server и bot) из boot-скрипта.

Рекомендуемо: использовать termux-services (как сервисы), чтобы был рестарт и логи.

9) Конфигурация (V1)
Обязательные параметры:

BOT_TOKEN — токен BotFather.

ALLOWED_USER_IDS — whitelist Telegram ID.

SHARED_DIR — куда сохранять видео (shared storage).

TMP_DIR — временная папка для .part.

TELEGRAM_API_ID, TELEGRAM_API_HASH — для запуска локального Bot API server.

Опциональные:

PAGE_SIZE

MAX_CONCURRENT_DOWNLOADS

SEND_AS (document|video)

LOG_LEVEL, LOG_PATH

10) Ограничения и допущения
Webhook наружу не используется, т.к. локальный Bot API server HTTP-only и требует отдельного TLS proxy для HTTPS; применяем long polling.

Bot должен работать постоянно, чтобы не терять updates (учитываем особенности хранения updates на стороне Bot API).

11) Критерии приёмки (Acceptance Criteria)
Пользователь из whitelist:

Нажимает 📥 Inbox → появляется “живое” сообщение списка; повторные нажатия обновляют то же сообщение.

Пересылает видео → файл появляется в SHARED_DIR полностью скачанным (атомарная запись).

Выбирает файл через inline → может скачать или удалить с подтверждением.

Пользователь вне whitelist:

не может просмотреть список, скачать, удалить или загрузить файлы.

После перезагрузки TV-box:

Bot API server и бот автоматически запускаются через Termux:Boot и отвечают пользователю.
​

Bot API server запущен с --local и получает api_id/api_hash; бот использует локальный сервер и умеет работать с абсолютными file_path из getFile.

Если нужно — могу в следующем сообщении превратить этот PRD в структуру репозитория (директории, конфиги, скрипты boot/services) и чеклист установки конкретно для твоего X96Q/slimBOXtv.

