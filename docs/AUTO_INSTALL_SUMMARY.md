# Финальная сводка: Автоматическая установка ffmpeg

## 🎯 Цель

Упростить установку бота и автоматизировать установку всех зависимостей, включая **ffmpeg**, который необходим для корректной работы с метаданными видео.

## ✅ Что сделано

### 1. Автоматический скрипт установки

**Файл:** `scripts/install_dependencies.sh`

**Возможности:**
- ✅ Автоматическое определение окружения (Termux/Debian/RHEL)
- ✅ Установка системных пакетов: Python, Git, **ffmpeg**
- ✅ Установка Python зависимостей из requirements.txt
- ✅ Создание необходимых директорий (logs/, tmp/)
- ✅ Настройка .env из .env.example
- ✅ Проверка успешности установки ffmpeg

**Использование:**
```bash
cd ~/telegram-video-inbox
chmod +x scripts/install_dependencies.sh
./scripts/install_dependencies.sh
```

### 2. Проверка ffmpeg при запуске бота

**Файл:** `bot/main.py`

**Изменения:**
- ✅ Добавлена проверка наличия ffprobe при старте бота
- ✅ Выводится предупреждение, если ffmpeg не установлен
- ✅ Подсказка по установке для разных систем

**Пример вывода при отсутствии ffmpeg:**
```
============================================================
⚠️  ffmpeg/ffprobe is NOT installed!
Video aspect ratio preservation may not work correctly.
Install with: pkg install ffmpeg (Termux)
           or apt install ffmpeg (Debian/Ubuntu)
Or run: ./scripts/install_dependencies.sh
============================================================
```

### 3. Обновленная документация

**README.md:**
- ✅ Добавлен раздел "Быстрая автоматическая установка"
- ✅ Инструкции по использованию install_dependencies.sh
- ✅ Сохранен раздел "Ручная установка" как альтернатива

**docs/INSTALLATION.md:**
- ✅ Добавлена TIP-секция с автоматической установкой
- ✅ Добавлена NOTE-секция про важность ffmpeg
- ✅ Пояснение о последствиях отсутствия ffmpeg

**docs/AUTO_INSTALL.md (НОВЫЙ):**
- ✅ Полное руководство по использованию скрипта
- ✅ Поддерживаемые платформы
- ✅ Примеры вывода
- ✅ Troubleshooting

**CHANGELOG.md:**
- ✅ Добавлена информация о новом скрипте
- ✅ Обновлен список файлов
- ✅ Описание новой функциональности

## 📊 Статистика изменений (дополнительно)

**Новые файлы:**
- `scripts/install_dependencies.sh` - скрипт автоматической установки (~185 строк)
- `docs/AUTO_INSTALL.md` - документация (~250 строк)

**Измененные файлы:**
- `bot/main.py` - добавлена проверка ffmpeg (~13 строк)
- `README.md` - добавлена автоматическая установка (~22 строки)
- `docs/INSTALLATION.md` - добавлена TIP и NOTE секции (~17 строк)
- `CHANGELOG.md` - обновлена информация (~30 строк)

**Всего изменений:**
- Новых файлов: 2
- Измененных файлов: 4
- Строк кода добавлено: ~517

## 🎁 Преимущества

### Для пользователя:
1. **Простота установки**: Один скрипт вместо множества команд
2. **Меньше ошибок**: Автоматическая проверка зависимостей
3. **Понятные предупреждения**: Бот сообщает, если ffmpeg не установлен
4. **Гибкость**: Можно использовать автоматическую или ручную установку

### Для разработчика:
1. **Легче поддержка**: Централизованная установка зависимостей
2. **Меньше вопросов**: Пользователи реже сталкиваются с проблемами
3. **Расширяемость**: Легко добавить новые зависимости в скрипт
4. **Кросс-платформенность**: Поддержка Termux, Debian, RHEL

## 🚀 Workflow установки (теперь)

### До изменений:
```bash
# 1. Установить Termux из F-Droid
# 2. Обновить пакеты
pkg upgrade
pkg install python git ffmpeg  # Пользователь мог забыть ffmpeg!

# 3. Клонировать
cd ~
git clone ...
cd telegram-video-inbox

# 4. Установить Python зависимости
pip install -r requirements.txt

# 5. Создать директории
mkdir -p logs tmp

# 6. Настроить .env
cp .env.example .env
nano .env
```

### После изменений:
```bash
# 1. Установить Termux из F-Droid
# 2. Клонировать
cd ~
git clone ...
cd telegram-video-inbox

# 3. Запустить автоматическую установку
chmod +x scripts/install_dependencies.sh
./scripts/install_dependencies.sh

# 4. Настроить .env (файл уже создан автоматически)
nano .env
```

**Упрощение:** 10+ команд → 4 команды

## 🧪 Проверка

### Проверить скрипт установки:
```bash
cd ~/telegram-video-inbox
./scripts/install_dependencies.sh
```

### Проверить предупреждение ffmpeg:
```bash
# Временно переименовать ffprobe (для теста)
sudo mv /usr/bin/ffprobe /usr/bin/ffprobe.bak

# Запустить бота
./scripts/start_bot.sh
# Должно появиться предупреждение

# Вернуть ffprobe
sudo mv /usr/bin/ffprobe.bak /usr/bin/ffprobe
```

### Проверить автоматическую установку .env:
```bash
# Удалить .env
rm .env

# Запустить скрипт
./scripts/install_dependencies.sh

# Проверить, что .env создан
ls -la .env
```

## 📝 Что дальше?

Пользователь может:

1. **Использовать автоматическую установку** (рекомендуется):
   ```bash
   ./scripts/install_dependencies.sh
   ```

2. **Использовать ручную установку** (если нужен контроль):
   ```bash
   pkg install python git ffmpeg
   pip install -r requirements.txt
   ```

3. **Обновить зависимости в любой момент**:
   ```bash
   git pull
   ./scripts/install_dependencies.sh
   ```

## 🎉 Итог

Теперь **ffmpeg автоматически устанавливается** при использовании скрипта `install_dependencies.sh`, а если пользователь установит бота вручную и забудет ffmpeg - бот выдаст понятное предупреждение при запуске.

**Проблема решена!** ✅

---

**Дата:** 2026-01-06  
**Версия:** 1.1.0 (автоматическая установка + проверка ffmpeg)
